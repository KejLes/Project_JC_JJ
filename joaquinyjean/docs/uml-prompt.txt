# UML Generation Prompt for GameVault

Use the following prompt with an AI assistant or a UML tool (like Mermaid.js or a specialized LLM) to generate a class diagram for GameVault:

---

Generate a comprehensive UML class diagram for the GameVault project using Mermaid.js syntax. The diagram must show the relationships (inheritance, implementation, association) between the following components:

1. **Interfaces**:
   - `Accionar` (videojuego actions)
   - `AccionSound` (soundtrack actions)
   - `Valorable` (if applicable)
   - `Exportable` (if applicable)

2. **Abstract Superclasses**:
   - `Videojuego` (implements Accionar)
   - `Soundtrack` (implements AccionSound)

3. **Concrete Subclasses**:
   - `Digital` and `Fisico` (extend Videojuego)
   - `SoundtrackVideojuego` (extends Soundtrack)

4. **Persistence Classes**:
   - `GestorFicherosCSV` (static methods for Videojuego persistence)
   - `GestorFicherosJSON` (static methods for Soundtrack persistence)

5. **Main Application**:
   - `App` (assembles everything, uses managers and collections)

**Details to include**:
- All private fields (id, nombre, etc.) with types.
- Key public methods: constructors, abstract methods (`mostrarInfo`, `obtenerFormatoDescripcion`), and specific methods like `toJSON()` and `escribirCSV()`.
- Collection types like `ArrayList<String>` for genres and `TreeMap<Integer, Videojuego>` in the main app logic.
- Ensure compatibility with tools like draw.io (by using the Mermaid import feature) or online Mermaid live editors.

---
